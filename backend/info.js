//Dependences installed

var dependencies = {
    "body-parser": "^1.19.0", //For parse package
    "connect-multiparty": "^2.2.0", //For use files in the backend
    "express": "^4.17.1", //For function backend 
    "mongoose": "^5.10.3", //For use mongo DB
    "nodemon": "^2.0.4" //For see the changes on the server
}

var package_json_scripts = {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index.js"
}